## What does this change?

<!-- What and why, in a few sentences. Link the issue it addresses. -->

## How was it tested?

<!-- Commands you ran, screenshots for UI changes. -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck` and `pnpm test` pass
- [ ] Documentation is updated if behaviour or configuration changed
- [ ] If the API changed: `pnpm sdk:generate` was run
- [ ] If the database schema changed: `pnpm db:generate` was run
