# Where your Arclight API runs, and an API key created on it (it starts with arc_).
ARCLIGHT_API_URL=http://localhost:8787
ARCLIGHT_API_KEY=
# Password for this example's admin pages. Your own admin uses its own sign-in instead.
ADMIN_PASSWORD=
